{
  "routes": [
    {
      "route": "/api/*",
      "rewrite": "https://asbnapp-api-prod.azurewebsites.net/api/*"
    }
  ],
  "navigationFallback": {
    "rewrite": "index.html",
    "exclude": [ "/images/*.{png,jpg,gif}", "/css/*" ]
  }
}