server {
  listen 80;
  
  location / {
    root   /usr/share/nginx/html;
    index  index.html;
    try_files $uri $uri/ /index.html =404; # This is required for Blazor 404 routing
  }

  # backend redirects
  location /api/ {
    proxy_pass ${API_PROXY_PASS_URL};
    proxy_http_version 1.1;
  }
}